server:
  port: 80

spring:
  application:
    name: shopping-gateway-service
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
    gateway:
      discovery:
        locator:
          enabled: true
  redis:
    host: localhost
    password:
    port: 6379
    database: 10

#gateway:
#  dynamicRoute:
#    enabled:  true
#    dataType: nacos
#    dataId: 'shopping_routes'
#    group: 'SHOPPING_GATEWAY'
